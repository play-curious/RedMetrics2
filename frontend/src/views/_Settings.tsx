import React from "react";

import * as types from "rm2-typings";

export default function _Settings({ user }: { user: types.tables.Account }) {
  return <h1> WIP (cookie management, theme color...) </h1>;
}
