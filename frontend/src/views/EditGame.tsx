import React from "react";

import * as types from "rm2-typings";

export default function EditGame({ user }: { user?: types.ApiKeyUser }) {
  return (
    <>
      <h1> Edit Game </h1>
    </>
  );
}
